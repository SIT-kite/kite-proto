syntax = "proto3";
package kite.ping;

message PingRequest {
    string text = 1;
}

message PongResponse {
    string text = 1;
}

service PingService {
    rpc Ping(PingRequest) returns (PongResponse);
}

